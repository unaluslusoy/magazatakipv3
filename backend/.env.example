# ============================================================================
# MağazaPano Backend - Environment Variables
# ============================================================================

# Application
NODE_ENV=development
PORT=3000
API_PREFIX=/api

# Database (Neon PostgreSQL)
DB_HOST=ep-flat-haze-a92w4l8l-pooler.gwc.azure.neon.tech
DB_PORT=5432
DB_NAME=magaza_takip
DB_USER=neondb_owner
DB_PASSWORD=npg_CmTYcM1ajG0e
DB_SSL=true

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=your-refresh-token-secret
JWT_REFRESH_EXPIRES_IN=7d

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=524288000
ALLOWED_VIDEO_FORMATS=mp4,webm,mov
ALLOWED_IMAGE_FORMATS=jpg,jpeg,png,gif,webp

# FFmpeg
FFMPEG_PATH=/usr/bin/ffmpeg
FFPROBE_PATH=/usr/bin/ffprobe

# CORS
CORS_ORIGIN=http://localhost:5000,http://localhost:8080
CORS_CREDENTIALS=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=debug
LOG_FILE=./logs/app.log

# Socket.IO
SOCKET_PORT=3001
SOCKET_CORS_ORIGIN=*

# Email (Optional - for password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@magazapano.com

# Storage (Future: S3/CloudFlare R2)
STORAGE_TYPE=local
# S3_BUCKET=
# S3_REGION=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
